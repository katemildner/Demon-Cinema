<%= form_with(model: cinema_hall) do |form| %>
  <% if cinema_hall.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cinema_hall.errors.count, "error") %> prohibited this cinema_hall from being saved:</h2>

      <ul>
        <% cinema_hall.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :number_of_rows, style: "display: block" %>
    <%= form.number_field :number_of_rows %>
  </div>

  <div>
    <%= form.label :number_of_columns, style: "display: block" %>
    <%= form.number_field :number_of_columns %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
